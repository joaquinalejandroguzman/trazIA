---
inclusion: manual
---

# Workflows y Comandos

Este archivo es de inclusión MANUAL — actívalo con `#workflows` en el chat.
Documenta cómo correr, testear y desplegar el proyecto para que Kiro use los comandos correctos.

---

## Instalación

<!-- Pasos para instalar el proyecto desde cero. Por ejemplo:
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```
-->

## Correr en desarrollo

<!-- Comando(s) para levantar el proyecto localmente. Por ejemplo:
```bash
uvicorn src.main:app --reload
# o
npm run dev
```
-->

## Tests

<!-- Cómo ejecutar los tests. Por ejemplo:
```bash
pytest tests/ -v
# o
npm run test
```
-->

## Build / Deploy

<!-- Cómo construir o desplegar. Por ejemplo:
```bash
docker build -t trazia .
docker-compose up -d
```
-->

## Variables de entorno

<!-- Lista las variables de entorno requeridas (sin sus valores). Por ejemplo:
- DATABASE_URL → conexión a PostgreSQL
- OPENAI_API_KEY → clave de la API de OpenAI
- SECRET_KEY → clave para JWT
- DEBUG → true/false
-->
