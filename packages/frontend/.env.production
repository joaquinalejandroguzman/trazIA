# Vacío a propósito: axios queda con baseURL '' y las llamadas salen
# relativas (/api/analyze), pegando al mismo origen que sirve el frontend.
# Ver packages/frontend/src/services/api_client.ts:4
VITE_API_URL=

# Backend real, sin mocks
VITE_USE_MOCK=false
